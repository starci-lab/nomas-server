import { Module } from "@nestjs/common"

/**
 * Pet Emitter Module
 * This module is kept for future emitter-related functionality
 * Event listeners are now directly in handlers/pet/pet.event-handler.ts
 */
@Module({})
export class PetEmitterModule {}
